/*
Задание 1:
1. Попробуй создать несколько переменных с разными типами данных.
2. Напиши короткий скрипт, который использует переменные и выводит их значения в консоль с помощью console.log().
*/

// Объявление переменных
let x = "Hello, world";    // string
let y = 42;                // number
let isHappy = true;        // boolean
let empty = null;          // null
let notDefined;            // undefined
let bigNum = 12345678901234567890n; // bigint
let array = [1, 2, 3];     // массив
let object = { key: "value" }; // объект

// Вывод значений в консоль
console.log(x);            // "Hello, world"
console.log(y);            // 42
console.log(isHappy);      // true
console.log(empty);        // null
console.log(notDefined);   // undefined
console.log(bigNum);       // 12345678901234567890n
console.log(array);        // [1, 2, 3]
console.log(object);       // { key: "value" }


/* Задание 2:
1. Напиши условие, которое проверяет, является ли число положительным, отрицательным или нулем.
2. Создай for цикл, который выводит числа от 1 до 10.
3. Добавь while цикл, который будет уменьшать переменную от 10 до 1.
*/


/* Задание 3: Создай функцию для сложения чисел
1. Попробуй создать функцию с именем `add`.
2. Пусть она принимает два параметра (например, `x` и `y`) и возвращает их сумму.
3. Вызови эту функцию, передав ей любые два числа.
/*


/* Задание 3_1:
1. Напиши функцию, которая принимает два числа и возвращает их сумму.
2. Напиши функцию, которая выводит сообщение в консоль.
3. Создай стрелочную функцию, которая возвращает квадрат числа.
/*


/* Задание 4:
1. Создай объект `car` с полями `brand`, `model`, и `year`.
2. Измени одно из свойств объекта и выведи его в консоль.
3. Создай массив `colors` из нескольких цветов, добавь один в конец и удали один из начала, выведи результат в консоль.
*/
